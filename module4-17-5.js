var penColor = 'red';
var quantity = 2;
var isActive = false;

// array

var items = ['watch', 'cap', 'shows', 'mobile'];

items.indexOf();

items.push('table');

items.pop();

if (condition) {
    
} else {
    
}